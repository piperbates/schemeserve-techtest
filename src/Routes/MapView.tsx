import React from 'react';
import StyledView from '../Components/StyledView.styled';

function MapView () {
    return (
        <StyledView>
            Map View
        </StyledView>
    );
}

export default MapView;